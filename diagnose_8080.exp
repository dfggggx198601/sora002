#!/usr/bin/expect -f

set password "440696ar3W!"
set host "root@141.147.147.229"
set timeout 60

spawn ssh -o StrictHostKeyChecking=no $host "docker ps; netstat -tulpn | grep 8080; ufw status; curl -I http://localhost:8080"
expect {
    "password:" { send "$password\r" }
    timeout { puts "SSH Timeout"; exit 1 }
}
expect eof
