#!/usr/bin/expect -f

set password "440696ar3W!"
set host "root@141.147.147.229"
set timeout 60

spawn ssh -o StrictHostKeyChecking=no $host "netstat -tulpn | grep :80; docker ps; ps aux | grep nginx"
expect {
    "password:" { send "$password\r" }
    timeout { puts "SSH Timeout"; exit 1 }
}
expect eof
